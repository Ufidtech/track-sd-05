// BUILD STEP 6/7 (mocked) — Notification delivery (PRD Section 5.1).
//
// For the capstone, this is MOCKED — no live SMS/IVR provider. This file
// should simulate sending a notification and simulate a delivery status
// response, so the rest of the system (strike counting, HELD status,
// degraded-channel flagging) can be built and tested against it.
//
// Written so a real provider (e.g. Termii, Africa's Talking) could be
// swapped in later without changing calling code elsewhere.

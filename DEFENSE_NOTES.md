# Defense Notes

Write a short entry here the SAME DAY you build or understand something —
not at the end. Plain language, like you're explaining it to someone who's
never seen the code. This file is what you re-read on Aug 21 instead of
trying to reconstruct your own reasoning from memory.

Format for each entry:
  ## [Concept/File]
  What it does:
  Why this way and not the simpler/obvious way:
  What breaks if we removed it:

---

## Postgres SEQUENCE / nextval()
What it does:
Why this way and not the simpler/obvious way:
What breaks if we removed it:

## Idempotency-Key
What it does:
Why this way and not the simpler/obvious way:
What breaks if we removed it:

## SSE heartbeat + fallback
What it does:
Why this way and not the simpler/obvious way:
What breaks if we removed it:

## Ticket lifecycle / EXPIRED state
What it does:
Why this way and not the simpler/obvious way:
What breaks if we removed it:
